# دور المتبرعين في النظام

## نظرة عامة
المتبرعون هم جزء أساسي من النظام. دورهم الرئيسي هو:
1. **رؤية طلبات SOS** التي تحتاج تبرع
2. **إرسال طلب للمستشفى** للتأكد من عملية التبرع
3. **متابعة حالاتهم** مع المستشفيات

---

## ما يمكن للمتبرع فعله:

### 1. رؤية طلبات SOS المتاحة (الحالات التي تحتاج تبرع)

#### Endpoint: `GET /api/available-sos-cases`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يعرض جميع طلبات SOS المفتوحة التي:
  - فصيلة دم المتبرع متوافقة مع فصيلة دم المريض
  - المتبرع قريب من موقع الطلب (ضمن المسافة المحددة في `sos_radius_km`)
  - الطلب لا يزال مفتوحاً (`status = 'open'`)

**Response:**
```json
{
  "status": 200,
  "message": "تم جلب الحالات المتاحة بنجاح",
  "data": {
    "items": {
      "available_cases": [
        {
          "id": 1,
          "patient": {
            "id": 2,
            "name": "أحمد محمد",
            "phone": "0503333333"
          },
          "type": "blood",
          "type_label": "دم",
          "blood_type": "O+",
          "description": "أحتاج تبرع دم عاجل",
          "distance_km": 2.5,
          "is_new": true,
          "created_at": "2025-11-24T10:00:00.000000Z"
        }
      ],
      "new_cases": [...],
      "summary": {
        "total": 3,
        "new_count": 1
      }
    }
  }
}
```

**مثال:**
```bash
curl -X GET http://localhost:8000/api/available-sos-cases \
  -H "Authorization: Bearer {donor_token}"
```

---

### 2. رؤية تاريخ التبرعات (طلبات SOS التي تم إرسال إشعارات لها)

#### Endpoint: `GET /api/donations`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يعرض جميع طلبات SOS التي:
  - المتبرع يمكنه التبرع لها (فصيلة دم متوافقة)
  - المتبرع قريب من موقع الطلب
  - تم إرسال إشعار للمتبرع عنها

**Response:**
```json
{
  "status": 200,
  "message": "تم جلب تاريخ التبرعات بنجاح",
  "data": {
    "items": [
      {
        "id": 1,
        "patient_id": 2,
        "patient_name": "أحمد محمد",
        "patient_phone": "0503333333",
        "type": "blood",
        "blood_type": "O+",
        "description": "أحتاج تبرع دم عاجل",
        "distance_km": 2.5,
        "status": "open",
        "created_at": "2025-11-24T10:00:00.000000Z"
      }
    ]
  }
}
```

---

### 3. إيجاد أقرب مستشفى

#### Endpoint: `GET /api/hospital-requests/find-nearest`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يبحث عن أقرب مستشفى بناءً على موقع المتبرع الحالي
- يعرض معلومات المستشفى (الاسم، العنوان، الإحداثيات)

**Response:**
```json
{
  "status": 200,
  "message": "تم العثور على أقرب مستشفى",
  "data": {
    "items": {
      "hospital": {
        "id": 1,
        "name": "مستشفى النور التخصصي",
        "address": "شارع الملك فهد، الرياض",
        "latitude": 24.7136,
        "longitude": 46.6753
      }
    }
  }
}
```

---

### 4. إرسال طلب للمستشفى (للتأكد من عملية التبرع)

#### Endpoint: `POST /api/hospital-requests/`
**Headers:** `Authorization: Bearer {donor_token}`

**Request:**
```json
{
  "hospital_id": 1,
  "sos_request_id": 1,
  "user_notes": "أنا جاهز للتبرع، فصيلة دمي O+"
}
```

**ما يحدث:**
- يرسل طلب للمستشفى من المتبرع
- المستشفى سيرى الطلب في Dashboard
- يمكن للمستشفى الموافقة أو الرفض
- عند الموافقة، يتم إرسال إشعار للمتبرع

**Response:**
```json
{
  "status": 201,
  "message": "تم إرسال الطلب للمستشفى بنجاح",
  "data": {
    "items": {
      "request_id": 2,
      "hospital_id": 1,
      "hospital_name": "مستشفى النور التخصصي",
      "status": "pending"
    }
  }
}
```

---

### 5. إرسال طلب لأقرب مستشفى تلقائياً

#### Endpoint: `POST /api/hospital-requests/nearest`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يجد أقرب مستشفى تلقائياً
- يرسل طلب للمستشفى مباشرة
- لا يحتاج تحديد `hospital_id`

**Response:**
```json
{
  "status": 201,
  "message": "تم إرسال الطلب لأقرب مستشفى بنجاح",
  "data": {
    "items": {
      "request_id": 2,
      "hospital": {
        "id": 1,
        "name": "مستشفى النور التخصصي",
        "address": "شارع الملك فهد، الرياض"
      },
      "status": "pending"
    }
  }
}
```

---

### 6. عرض طلباتي للمستشفيات

#### Endpoint: `GET /api/hospital-requests/my-requests`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يعرض جميع طلبات المتبرع للمستشفيات
- يفصل الطلبات حسب الحالة (pending, approved, rejected)

**Response:**
```json
{
  "status": 200,
  "message": "تم جلب الطلبات بنجاح",
  "data": {
    "items": [
      {
        "id": 2,
        "hospital": {
          "id": 1,
          "name": "مستشفى النور التخصصي",
          "address": "شارع الملك فهد، الرياض"
        },
        "request_type": "donner",
        "status": "approved",
        "user_notes": "أنا جاهز للتبرع",
        "notes": "تم التأكد من فصيلة الدم، جاهز للتبرع",
        "created_at": "2025-11-24T10:00:00.000000Z"
      }
    ]
  }
}
```

---

### 7. عرض الحالات المتاحة (طلباتي مع المستشفيات)

#### Endpoint: `GET /api/hospital-requests/available-cases`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يعرض جميع طلبات المتبرع للمستشفيات
- يفصل الطلبات حسب الحالة:
  - `pending`: في الانتظار
  - `approved`: موافق عليه
  - `rejected`: مرفوض
  - `new_cases`: حالات جديدة (تم الموافقة عليها خلال 24 ساعة)

**Response:**
```json
{
  "status": 200,
  "message": "تم جلب الحالات المتاحة بنجاح",
  "data": {
    "items": {
      "all_requests": [...],
      "pending": [...],
      "approved": [...],
      "rejected": [...],
      "new_cases": [...],
      "summary": {
        "total": 3,
        "pending_count": 1,
        "approved_count": 1,
        "rejected_count": 1,
        "new_cases_count": 1
      }
    }
  }
}
```

---

### 8. عدد الحالات الجديدة

#### Endpoint: `GET /api/hospital-requests/new-count`
**Headers:** `Authorization: Bearer {donor_token}`

**ما يحدث:**
- يعرض عدد الحالات الجديدة (للعرض في badge)
- الحالات الجديدة = طلبات تم الموافقة عليها خلال آخر 24 ساعة

**Response:**
```json
{
  "status": 200,
  "message": "تم جلب عدد الحالات الجديدة",
  "data": {
    "items": {
      "new_cases_count": 2,
      "pending_count": 1,
      "total_new": 3
    }
  }
}
```

---

### 9. تحديث الموقع

#### Endpoint: `POST /api/location`
**Headers:** `Authorization: Bearer {donor_token}`

**Request:**
```json
{
  "lat": 24.7236,
  "lng": 46.6853
}
```

**ما يحدث:**
- يحدث موقع المتبرع
- هذا مهم لرؤية طلبات SOS القريبة
- مهم لإيجاد أقرب مستشفى

---

## السيناريو الكامل للمتبرع:

### الخطوة 1: تسجيل الدخول
```bash
POST /api/auth/login
{
  "email": "donor1@test.com",
  "password": "password123"
}
```

### الخطوة 2: تحديث الموقع
```bash
POST /api/location
{
  "lat": 24.7236,
  "lng": 46.6853
}
```

### الخطوة 3: رؤية الحالات المتاحة (طلبات SOS)
```bash
GET /api/available-sos-cases
```
**النتيجة:** يرى طلبات SOS التي يمكنه التبرع لها

### الخطوة 4: إيجاد أقرب مستشفى
```bash
GET /api/hospital-requests/find-nearest
```
**النتيجة:** يعرف أقرب مستشفى له

### الخطوة 5: إرسال طلب للمستشفى
```bash
POST /api/hospital-requests/
{
  "hospital_id": 1,
  "sos_request_id": 1,
  "user_notes": "أنا جاهز للتبرع"
}
```
**النتيجة:** يرسل طلب للمستشفى للتأكد من عملية التبرع

### الخطوة 6: متابعة حالاتي
```bash
GET /api/hospital-requests/available-cases
```
**النتيجة:** يرى حالة طلباته (موافق عليه، مرفوض، في الانتظار)

---

## ملاحظات مهمة:

1. **المتبرع لا يمكنه إنشاء طلب SOS** - فقط المريض يمكنه ذلك
2. **المتبرع يرى فقط طلبات SOS المتوافقة** مع فصيلة دمه
3. **المتبرع يجب أن يكون قريباً** من موقع الطلب (ضمن `sos_radius_km`)
4. **عند الموافقة من المستشفى**، يتم إرسال إشعار للمتبرع
5. **المتبرع يمكنه إرسال طلب للمستشفى** حتى لو لم يكن هناك طلب SOS

---

## مثال عملي:

### المتبرع يرى حالة جديدة:

1. **مريض ينشئ طلب SOS** → يتم إرسال إشعار للمتبرعين القريبين
2. **المتبرع يفتح التطبيق** → يرى الحالة في `/api/available-sos-cases`
3. **المتبرع يقرر التبرع** → يرسل طلب للمستشفى
4. **المستشفى يوافق** → يتم إرسال إشعار للمتبرع
5. **المتبرع يرى الموافقة** → في `/api/hospital-requests/available-cases`

---

## الخلاصة:

المتبرعون هم **الجسر بين المرضى والمستشفيات**:
- ✅ يرون الحالات التي تحتاج تبرع
- ✅ يرسلون طلبات للمستشفيات للتأكد
- ✅ يتابعون حالاتهم
- ✅ يتلقون إشعارات عند الموافقة/الرفض

**بدون المتبرعين، لن يعمل النظام!** 🩸

